// OTP model removed.
// The OTP / message-sending feature has been deleted from the codebase.
// If you need to implement a new OTP or messaging model, add it here.

// Export null as a placeholder so any require(...) calls won't throw.
module.exports = null;